[package]
edition = "2018"
name = "random-seed-future"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
futures = "0.3"

[dev-dependencies]
async-std = {version = "1.9.0", features = ["attributes"]}
